# vim: tabstop=8 shiftwidth=8 noexpandtab:

TOPDIR = $(realpath ..)

SUBDIR = cat chmod date echo ksh ln ls lua mandelbrot mish mkdir pwd \
	 rm rmdir sandbox test_kbd test_rtc utest

all: build

include $(TOPDIR)/build/common.mk
