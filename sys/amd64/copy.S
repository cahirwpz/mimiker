#include <amd64/asm.h>

ENTRY(copyinstr)
	# not implemented
	mov 0, %rax
END(copyinstr)

ENTRY(copyin)
	# not implemented
	mov 0, %rax
END(copyin)

ENTRY(copyout)
	# not implemented
	mov 0, %rax
END(copyout)

ENTRY(try_load_word)
	# not implemented
	mov 0, %rax
END(try_load_word)

ENTRY(try_store_word)
	# not implemented
	mov 0, %rax
END(try_store_word)

# vim: sw=8 ts=8 et
